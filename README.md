# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- Пользователь способен включать и выключать отопление в доме.
- Система поддерживает добавление/удаление и обновление информации о сенсорах

**Мониторинг температуры:**

- Пользователи могут просматривать текущую температуру в своих домах
- Система поддерживает получение информации о температуре по Id Сенсора или Id локации

Сервис sensors имеет следующие ручки
```
GET /sensors - Получить список всех датчиков
GET /sensors/:id - Получить конкретный датчик по ID
POST /sensors - Создать новый датчик
PUT /sensors/:id - Полное обновление датчика
DELETE /sensors/:id - Удалить датчик
PATCH /sensors/:id/value - Обновить показания датчика
GET /sensors/temperature/:location - Получить температуру по местоположению
```

### 2. Анализ архитектуры монолитного приложения

Перечислите здесь основные особенности текущего приложения: какой язык программирования используется, какая база данных, как организовано взаимодействие между компонентами и так далее.

* **Язык программирования:** Go
* **База данных:** PostgreSQL
* **Архитектура:** Монолитная, все компоненты системы (обработка запросов, бизнес-логика, работа с данными) находятся в рамках одного приложения.

Сервис sensors не имеет чётко определённой зоны ответственности:
1. В коде явно не показано, как осуществляется взаимодействие с физическими датчиками. По имеющимся предположениям:
   - Пользователь через пользовательский интерфейс инициирует запрос к сервису sensors на изменение состояния устройства (например, температуры или питания);
   - Сервис sensors обрабатывает запрос, вызывая соответствующий хендлер;
   -Обработчики запросов на чтение обращаются к temperature-api, тогда как обработчики, изменяющие состояние, вероятно, воздействуют на устройство через специализированный API и одновременно вносят изменения в базу данных Sensors.
2. Модели данных недостаточно информативны:
   - В коде отсутствует явная связь между сущностью sensor и владельцем устройства (например, через home_id, обозначающий дом или квартиру пользователя);
   - Поле location ассоциировано с sensor_id и фактически выступает в роли его псевдонима;
   -Отсутствие home_id затрудняет понимание того, как сервис sensors идентифицирует устройство при запросе на изменение его состояния. Предполагается, что location служит инкапсуляцией, связывающей идентификатор дома пользователя и конкретную зону, в которой установлен сенсор. Например: /user/100/home/220/location/kitchen.



### 3. Определение доменов и границы контекстов

Опишите здесь домены, которые вы выделили.

Домен: **Управление Датчиками**
- Поддомены:
    - **Управление сенсорами**
        - Контексты: Управление состояниями устройств и их добавление/отвязка.
    - **Мониторинг**
       - Контексты: Получение данных о температуре

### **4. Проблемы монолитного решения**

* **Ограниченная масштабируемость**. "Агрессивный" мониторинг температуры может требовать больше ресурсов для
  выполнения агрегированных запросов. В это же время управление устройствами от пользователя явление не частое:
  установка либо демонтаж сенсора - запросы, которые можно рассчитывать не в пределах секунд, а в месяцах. Редактирование состояния сенсора - в пределах дня. 
* **Синхронное взаимодействие**, запросы обрабатываются последовательно.
* **Развертывание**: Требует остановки всего приложения.

### 5. Визуализация контекста системы — диаграмма С4

Добавьте сюда диаграмму контекста в модели C4.

 [C4_Context](diagrams/monolith/context_smart_home.png)

 # Задание 2. Проектирование микросервисной архитектуры

К диаграммам так же приложены png-изображения в соответствующих путях

**Диаграмма контейнеров (Containers)**

[smart_home](diagrams/microservices/containers/containers_smart_home.png)

**Диаграмма компонентов (Components)**

[device_manager_service](diagrams/microservices/components/device.png)
[telemetry](diagrams/microservices/components/telemetry.png)
[home_management](diagrams/microservices/components/home.png)
[user](diagrams/microservices/components/user.png)

**Диаграмма кода (Code)**

[device_manager_service](diagrams/microservices/code/device_manager_service.png)
[telemetry](diagrams/microservices/code/telemetry.png)

# Задание 3. Разработка ER-диаграммы

[smart_home_er](diagrams/microservices/er/smart_home.png)

# Задание 4. Создание и документирование API

### 1. Тип API

С учётом текущих условий и имеющихся данных, в архитектуре as-is будет использоваться синхронный API.
1. Сервис DeviceManager будет предоставлять ручки для управления устройствами, их состоянием и отправки команд. Для обеспечения плавного перехода от монолитной архитектуры к микросервисной, DeviceManager будет выполнять следующие функции:
- `Sensors` перенесен из монолитного приложения и интегрирован в `DeviceManager`, все эндпоинты будут доступны по пути `/api/v1/sensors`
- `DeviceManager` предоставляет эндпоинты для управления устройствами: создание/отвязка, получении информации, управление состояниями и отправка команд
- Внутри сервиса постепенно появится интерфейс к MQTT, который будет асинхронно отправлять команды на устройства, но это не является частью API самого сервиса. Такова
  будет особенность его реализации.
2. Сервис `Telemetry` будет отвечать за получение метрик с устройств. Его поведение включает:
- Получение метрик с сенсоров 
- Получение метрик с других типов устройств
- Сервис Telemetry запускает фоновый процесс, который периодически опрашивает temperature-api и сохраняет метрики в оперативной памяти (до тех пор, пока не будет подключено постоянное хранилище).

Синхронный API подходит как временное решение до полной миграции старой версии API к новой: пользователей сервиса не так много + крайне редкие изменения в SensorsDB.
Как видно на диаграмме, с появлением новых типов устройств, в частности тех, которые управляются командами, отправка команд и изменение состояний будут происходить *асинхронно*, через событийную шину, такую как MQTT. Такой же подход следует использовать и для сенсоров.
Когда весь сервис `devices` перейдет на MQTT, потребуется введение рабочего пула, который будет считывать результаты из MQTT и обновлять репозиторий состояний. Это минимизирует риск прерывания запросов из-за таймаутов, которые могут возникнуть при непосредственном взаимодействии с устройствами.

При переходе к микросервису возникает проблема: `Telemetry` должен иметь доступ к актуальному списку сенсоров и устройств. Как временное решение, `Telemetry`
периодически будет обращаться к API `DeviceManagerService` для получения актуального списка устройств.
Для `Telemetry` проблему можно решить с помощью паттерна Transactional Outbox. Этот подход гарантирует, что сервис не упустит новые и удаленные устройства при обновлении в репозитории `Devices`, а также позволит избежать перегрузки этой базы данных запросами на чтение.

### 2. Документация API

[device_manager_service](swagger/device_manager.yaml)
[telemetry](swagger/telemetry.yaml)
[user](swagger/user.yaml)
[home](swagger/home.yaml)

# Задание 5. Работа с docker и docker-compose

1. [temperature-api](apps/temperature-api/main.go)
2. [Dockerfile](apps/temperature-api/Dockerfile)

Запуск Dockerfile интегрирован в [docker-compose.yml](apps/docker-compose.yml), а значит запускается черех [init.sh](apps/init.sh)

# **Задание 6. Разработка MVP**

Разбиение по микросервисам располагается в папке `apps/smart_home_decomposed`

1. [devices](apps/smart_home_decomposed/devices/main.go)
2. [telemetry](apps/smart_home_decomposed/telemetry/main.go)

- Сервис `devices` использует эндпоинты монолитного `api/v1/sensors` для поддержания обратной совместимости и перехода. Также сервис представляет новые эндпоинты
  для ручек "devices" которые пока представляют собой наивную inmemory-реализацию
- Сервис `telemetry` предоставляет эндпоинты для считывания метрик с сенсоров. Наивная реализация данного микросервиса использует `sensors-api` для получения списка
  идентификаторов устройств.

 Микросервисы запускается этим же `docker-compose.yml` файлом для упрощения запуска. Они доступны по портам 8082 и 8083